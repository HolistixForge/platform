UI:

figma
storybook
react scss

fullstack:

upgrade tools: NX, deps
cjs / esm / esbuild / tsc / bundler / sass
package versioning
apps profiling
performances: lazy loading, chunks, splashscreen
CI tools: compile, tests

infra:

neo4j
AWS
