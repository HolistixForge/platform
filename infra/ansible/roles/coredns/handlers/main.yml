---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart CoreDNS
  ansible.builtin.systemd_service:
    name: coredns
    state: restarted
    daemon_reload: true
