# Run linter on staged files (includes ESLint TypeScript checking)
npx lint-staged

# Note: Full TypeScript compilation and tests run in CI
# Pre-commit hook is kept fast for better developer experience

